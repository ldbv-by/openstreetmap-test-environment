version: "3.8"

services:
  josm:
    container_name: josm
    build:
      context: ./josm
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    network_mode: "host"
    shm_size: "2g"
